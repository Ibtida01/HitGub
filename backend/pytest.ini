[pytest]
# Add the parent of backend/ (i.e. HitGub/) to sys.path.
# This makes `backend` importable as a package from anywhere inside backend/.
pythonpath = ..

# auto mode: @pytest.mark.asyncio is optional on each test.
# Tests that already have it continue to work unchanged.
asyncio_mode = auto